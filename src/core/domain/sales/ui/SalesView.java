package core.domain.sales.ui;

import core.shared.ui.console.*;

public class SalesView extends ConsoleRenderer {
    /**
     * Public constructor, needs a builder for
     * formats and stylized outputs
     *
     * @param builder
     */
    public SalesView(ConsoleBuilder builder) {
        super(builder);
    }
}
